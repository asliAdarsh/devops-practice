version: '3.8'
services:
  web:
    image: alpine:latest
    command: sh -c "apk add --no-cache stress && sleep 9999"
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 128M
        reservations:
          cpus: '0.25'
          memory: 64M
