version: '3.8'
services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    networks:
      - app-net

  db:
    image: alpine
    command: sleep infinity
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
